# Mini-lead-CRM
Mini Lead CRM is a backend REST API that manages potential customers (leads) through different stages of a sales pipeline. It supports CRUD operations, status transitions, and validation to track and convert leads efficiently.
